spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codearena
    username: codearena
    password: devpassword
  jpa:
    hibernate:
      ddl-auto: validate   # never "update" in a real project — migrations only